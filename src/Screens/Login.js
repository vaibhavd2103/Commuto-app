import { StyleSheet, View } from "react-native";
import React from "react";
import {
  Container,
  Heading,
  SubHeading,
  Simple,
} from "../components/GlobalComponents";

const Login = () => {
  return (
    <Container>
      <Heading style={{}}>Hello </Heading>
      <Simple>Walla habibi</Simple>
    </Container>
  );
};

export default Login;

const styles = StyleSheet.create({});
